let car = { brand: 'Cultus', model: 'Camry', year: 2024};

function carInfo(car) {
    return `The ${car.brand} ${car.model} was manufactured in ${car.year}.`;
}

console.log(carInfo(car)); 

